import React from 'react';
import classes from '../pages.module.scss';

const HomePage = () => {
    return (
        <div className={classes.pages}>
            <div className={classes.pages__title}>Главная страница</div>
        </div>
    );
};

export default HomePage;
